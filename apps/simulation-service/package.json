{
  "name": "unimplemented-service",
  "version": "1.0.0",
  "dependencies": {
    "express": "^4.18.2"
  },
  "scripts": {
    "start:prod": "node dist/main.js",
    "build": "mkdir -p dist && echo \"console.log('Service pending...'); setInterval(() => {}, 60000);\" > dist/main.js"
  }
}
